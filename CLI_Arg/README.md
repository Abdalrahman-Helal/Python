# CLI_Arg

## Task
Write a Python program to get the command-line arguments.

## Hint
Import `sys` and use `sys.argv`.

## What the script does
- Prints the script name or path.
- Prints the total number of arguments.
- Prints the full argument list.

## Run
```bash
python CLI_Arg.py arg1 arg2 arg3
```
